# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.env
# environment variables files ; 
# we should commit only the necessary files to the github so that 
# when another person uses our code , he needs to install the node packages 
# which are same as per our project -- and the command that will be ussed her is 
# npm install 
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
